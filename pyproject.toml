[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "integration: tests requiring GenLayer Studio (run with gltest)",
    "slow: nondeterministic live-LLM review tests (Stage 5); run with gltest -m slow",
]
